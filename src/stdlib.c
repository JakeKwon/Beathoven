// clang -emit-llvm -o stdlib.bc -c stdlib.c
#include "beathoven.h"

/*
extern struct pitch p;

void pp() {
    printf("%d", p.octave);
    printf("%d", p.alter);
}
*/
/*
extern void f(struct pitch p);

void g() {
    f(p);
}
int main() {
    return 0;
}
*/
